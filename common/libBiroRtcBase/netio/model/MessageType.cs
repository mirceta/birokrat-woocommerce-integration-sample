namespace si.birokrat.rtc.netio.model {
    public enum MessageType {
        QUIT = 0,
        PING = 1,
        ERROR = 2,
        MESSAGE = 3,
        ACK = 4,
        LOGGER = 5
    }
}
