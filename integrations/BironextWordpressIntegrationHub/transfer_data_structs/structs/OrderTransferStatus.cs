namespace validator {
    public enum OrderTransferStatus { 
        UNACCEPTED,
        ACCEPTED,
        ERROR,
        UNVERIFIED,
        VERIFIED,
        VERIFICATION_ERROR,
        NO_EVENT
    }
}
